{
  "id": "decode-ways",
  "title": "Decode Ways",
  "difficulty": "medium",
  "category": "dp-1d",
  "description": "You have intercepted a secret message encoded as a string of numbers. The message is decoded via the following mapping:\n\n\"1\" -> 'A'\n\n\"2\" -> 'B'\n\n...\n\n\"25\" -> 'Y'\n\n\"26\" -> 'Z'\n\nHowever, while decoding the message, you realize that there are many different ways you can decode the message because some codes are contained in other codes (`\"2\"` and `\"5\"` vs `\"25\"`).\n\nFor example, `\"11106\"` can be decoded into:\n\n\t- `\"AAJF\"` with the grouping `(1, 1, 10, 6)`\n\n\t- `\"KJF\"` with the grouping `(11, 10, 6)`\n\n\t- The grouping `(1, 11, 06)` is invalid because `\"06\"` is not a valid code (only `\"6\"` is valid).\n\nNote: there may be strings that are impossible to decode.\n\nGiven a string s containing only digits, return the number of ways to decode it. If the entire string cannot be decoded in any valid way, return `0`.\n\nThe test cases are generated so that the answer fits in a 32-bit integer.\n\n \n\nExample 1:\n\nInput: s = \"12\"\n\nOutput: 2\n\nExplanation:\n\n\"12\" could be decoded as \"AB\" (1 2) or \"L\" (12).\n\nExample 2:\n\nInput: s = \"226\"\n\nOutput: 3\n\nExplanation:\n\n\"226\" could be decoded as \"BZ\" (2 26), \"VF\" (22 6), or \"BBF\" (2 2 6).\n\nExample 3:\n\nInput: s = \"06\"\n\nOutput: 0\n\nExplanation:\n\n\"06\" cannot be mapped to \"F\" because of the leading zero (\"6\" is different from \"06\"). In this case, the string is not a valid encoding, so return 0.",
  "examples": [
    {
      "input": "s = \"12\"",
      "output": "2",
      "explanation": "\"12\" could be decoded as \"AB\" (1 2) or \"L\" (12)."
    },
    {
      "input": "s = \"226\"",
      "output": "3",
      "explanation": "\"226\" could be decoded as \"BZ\" (2 26), \"VF\" (22 6), or \"BBF\" (2 2 6)."
    },
    {
      "input": "s = \"06\"",
      "output": "0",
      "explanation": "\"06\" cannot be mapped to \"F\" because of the leading zero (\"6\" is different from \"06\"). In this case, the string is not a valid encoding, so return 0."
    }
  ],
  "constraints": [
    "`1 <= s.length <= 100`",
    "`s` contains only digits and may contain leading zero(s)."
  ],
  "execution_mode": "function",
  "class_name": "",
  "function_name": "num_decodings",
  "starter_code": "def num_decodings(s):\n    pass\n",
  "parameters": [
    {
      "name": "s",
      "type": "str"
    }
  ],
  "expected_return_type": "int",
  "comparison": "",
  "visible_tests": [
    {
      "args": [
        "12"
      ],
      "expected": 2
    },
    {
      "args": [
        "226"
      ],
      "expected": 3
    },
    {
      "args": [
        "06"
      ],
      "expected": 0
    }
  ],
  "hidden_tests": [],
  "hint_plan": {
    "level_1": "Break down the core operation in Decode Ways.",
    "level_2": "Look for the data structure or traversal pattern that avoids repeated work.",
    "level_3": "Use the constraints and examples to narrow the intended time complexity.",
    "level_4": "Implement the direct approach that maintains the required invariant step by step."
  },
  "canonical_solution_summary": "Implement the direct approach that maintains the required invariant step by step.",
  "disallowed_full_solution_exposure": true,
  "solution_sentences": [
    "def num_decodings(s):",
    "    if not s or s[0] == '0':",
    "        return 0",
    "    n = len(s)",
    "    dp = [0] * (n + 1)",
    "    dp[0] = 1",
    "    dp[1] = 1",
    "    for i in range(2, n + 1):",
    "        if s[i - 1] != '0':",
    "            dp[i] += dp[i - 1]",
    "        two_digit = int(s[i - 2:i])",
    "        if 10 <= two_digit <= 26:",
    "            dp[i] += dp[i - 2]",
    "    return dp[n]"
  ],
  "hints_per_sentence": [
    "Start by defining the function with the given name and parameter.",
    "Check if the string is empty or starts with '0', as these cases have no valid decodings.",
    "Return 0 immediately for those invalid starting conditions.",
    "Store the length of the string to use in loops and array sizes.",
    "Initialize a dynamic programming array with zeros, sized to handle substrings up to length n.",
    "Set the base case for an empty substring: there's exactly 1 way to decode nothing.",
    "Set the base case for the first character: since we already checked it's not '0', there's 1 way.",
    "Loop from the second character to the end, processing each position as a potential end of a substring.",
    "If the current character is not '0', it can stand alone as a single-digit code.",
    "Add the number of ways from the previous position to the current position's count.",
    "Extract the two-digit number formed by the current and previous characters.",
    "Check if this two-digit number is between 10 and 26, inclusive, making it a valid double-digit code.",
    "If valid, add the number of ways from two positions back to the current count.",
    "After the loop, return the total ways stored for the full string length."
  ],
  "final_explanation": "This solution uses dynamic programming to count decodings by building up from smaller substrings. At each position, we consider if the last digit can be a single code (if not '0') and if the last two digits form a valid double-digit code (10-26), adding ways from previous states. The base cases handle empty strings and initial zeros, ensuring invalid sequences return 0."
}
