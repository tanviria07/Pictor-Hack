{
  "id": "last-stone-weight",
  "title": "Last Stone Weight",
  "difficulty": "easy",
  "category": "heap-priority-queue",
  "description": "You are given an array of integers `stones` where `stones[i]` is the weight of the `i^th` stone.\n\nWe are playing a game with the stones. On each turn, we choose the heaviest two stones and smash them together. Suppose the heaviest two stones have weights `x` and `y` with `x <= y`. The result of this smash is:\n\n\t- If `x == y`, both stones are destroyed, and\n\n\t- If `x != y`, the stone of weight `x` is destroyed, and the stone of weight `y` has new weight `y - x`.\n\nAt the end of the game, there is at most one stone left.\n\nReturn the weight of the last remaining stone. If there are no stones left, return `0`.\n\n \n\nExample 1:\n\nInput: stones = [2,7,4,1,8,1]\nOutput: 1\nExplanation: \nWe combine 7 and 8 to get 1 so the array converts to [2,4,1,1,1] then,\nwe combine 2 and 4 to get 2 so the array converts to [2,1,1,1] then,\nwe combine 2 and 1 to get 1 so the array converts to [1,1,1] then,\nwe combine 1 and 1 to get 0 so the array converts to [1] then that's the value of the last stone.\n\nExample 2:\n\nInput: stones = [1]\nOutput: 1",
  "examples": [
    {
      "input": "stones = [2,7,4,1,8,1]",
      "output": "1",
      "explanation": "We combine 7 and 8 to get 1 so the array converts to [2,4,1,1,1] then,\nwe combine 2 and 4 to get 2 so the array converts to [2,1,1,1] then,\nwe combine 2 and 1 to get 1 so the array converts to [1,1,1] then,\nwe combine 1 and 1 to get 0 so the array converts to [1] then that's the value of the last stone."
    },
    {
      "input": "stones = [1]",
      "output": "1"
    }
  ],
  "constraints": [
    "`1 <= stones.length <= 30`",
    "`1 <= stones[i] <= 1000`"
  ],
  "execution_mode": "function",
  "class_name": "",
  "function_name": "last_stone_weight",
  "starter_code": "def last_stone_weight(stones):\n    pass\n",
  "parameters": [
    {
      "name": "stones",
      "type": "List[int]"
    }
  ],
  "expected_return_type": "int",
  "comparison": "",
  "visible_tests": [
    {
      "args": [
        [
          2,
          7,
          4,
          1,
          8,
          1
        ]
      ],
      "expected": 1
    },
    {
      "args": [
        [
          1
        ]
      ],
      "expected": 1
    }
  ],
  "hidden_tests": [],
  "hint_plan": {
    "level_1": "Simulate the process.  We can do it with a heap, or by sorting some list of stones every time we take a turn.",
    "level_2": "Simulate the process.  We can do it with a heap, or by sorting some list of stones every time we take a turn.",
    "level_3": "Simulate the process.  We can do it with a heap, or by sorting some list of stones every time we take a turn.",
    "level_4": "Simulate the process.  We can do it with a heap, or by sorting some list of stones every time we take a turn."
  },
  "canonical_solution_summary": "Simulate the process.  We can do it with a heap, or by sorting some list of stones every time we take a turn.",
  "disallowed_full_solution_exposure": true
}
