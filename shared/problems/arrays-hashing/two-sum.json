{
  "id": "two-sum",
  "title": "Two Sum",
  "difficulty": "easy",
  "category": "arrays-hashing",
  "description": "Given an array of integers `nums` and an integer `target`, return indices of the two numbers such that they add up to `target`.\n\nYou may assume that each input would have exactly one solution, and you may not use the same element twice.\n\nYou can return the answer in any order.\n\n \n\nExample 1:\n\nInput: nums = [2,7,11,15], target = 9\nOutput: [0,1]\nExplanation: Because nums[0] + nums[1] == 9, we return [0, 1].\n\nExample 2:\n\nInput: nums = [3,2,4], target = 6\nOutput: [1,2]\n\nExample 3:\n\nInput: nums = [3,3], target = 6\nOutput: [0,1]",
  "examples": [
    {
      "input": "nums = [2,7,11,15], target = 9",
      "output": "[0,1]",
      "explanation": "Because nums[0] + nums[1] == 9, we return [0, 1]."
    },
    {
      "input": "nums = [3,2,4], target = 6",
      "output": "[1,2]"
    },
    {
      "input": "nums = [3,3], target = 6",
      "output": "[0,1]"
    }
  ],
  "constraints": [
    "`2 <= nums.length <= 10^4`",
    "`-10^9 <= nums[i] <= 10^9`",
    "`-10^9 <= target <= 10^9`",
    "Only one valid answer exists."
  ],
  "execution_mode": "function",
  "class_name": "",
  "function_name": "two_sum",
  "starter_code": "def two_sum(nums, target):\n    pass\n",
  "parameters": [
    {
      "name": "nums",
      "type": "List[int]"
    },
    {
      "name": "target",
      "type": "int"
    }
  ],
  "expected_return_type": "List[int]",
  "comparison": "",
  "visible_tests": [
    {
      "args": [
        [
          2,
          7,
          11,
          15
        ],
        9
      ],
      "expected": [
        0,
        1
      ]
    },
    {
      "args": [
        [
          3,
          2,
          4
        ],
        6
      ],
      "expected": [
        1,
        2
      ]
    },
    {
      "args": [
        [
          3,
          3
        ],
        6
      ],
      "expected": [
        0,
        1
      ]
    }
  ],
  "hidden_tests": [],
  "hint_plan": {
    "level_1": "A really brute force way would be to search for all possible pairs of numbers but that would be too slow. Again, it's best to try out brute force solutions just for completeness. It is from these brute force solutions that you can come up with optimizations.",
    "level_2": "So, if we fix one of the numbers, say `x`, we have to scan the entire array to find the next number `y` which is `value - x` where value is the input parameter. Can we change our array somehow so that this search becomes faster?",
    "level_3": "The second train of thought is, without changing the array, can we use additional space somehow? Like maybe a hash map to speed up the search?",
    "level_4": "The second train of thought is, without changing the array, can we use additional space somehow? Like maybe a hash map to speed up the search?"
  },
  "company_tags": ["Google", "Amazon", "Microsoft"],
  "canonical_solution_summary": "The second train of thought is, without changing the array, can we use additional space somehow? Like maybe a hash map to speed up the search?",
  "disallowed_full_solution_exposure": true,
  "solution_sentences": [
    "def two_sum(nums, target):",
    "    seen = {}",
    "    for i, num in enumerate(nums):",
    "        complement = target - num",
    "        if complement in seen:",
    "            return [seen[complement], i]",
    "        seen[num] = i"
  ],
  "hints_per_sentence": [
    "Start by defining the function with the given parameters: a list of integers `nums` and an integer `target`.",
    "Initialize an empty dictionary to store numbers you've seen so far and their indices.",
    "Loop through the list using `enumerate` to get both the index `i` and the value `num` for each element.",
    "Calculate the number that, when added to `num`, equals the `target`; call this the complement.",
    "Check if this complement is already in the dictionary of seen numbers.",
    "If it is, return a list containing the index of the complement (from the dictionary) and the current index `i`.",
    "Otherwise, add the current number and its index to the dictionary for future checks."
  ],
  "final_explanation": "This solution uses a hash map (dictionary) to store each number and its index as we iterate through the list. For each number, we compute its complement (target minus the number) and check if it's already in the map. If found, we return the indices; otherwise, we add the current number to the map. This approach runs in O(n) time and O(n) space, efficiently solving the problem in one pass."
}
