{
  "id": "precode-dz-07-two-sum-brute",
  "slug": "precode-dz-07-two-sum-brute",
  "title": "Pair Indices For Target Sum",
  "difficulty": "medium",
  "category": "precode-dicts-sets",
  "description": "You have a list of integers `nums` and a `target`. Return indices `[i, j]` with `i < j` such that `nums[i] + nums[j] == target`. If no such pair exists, return `[]`. If several pairs work, return the first one found when you try `i` from left to right, then `j` after `i`.",
  "examples": [
    {
      "input": "[2,7,11,15], 9",
      "output": "[0, 1]"
    }
  ],
  "constraints": [],
  "execution_mode": "function",
  "class_name": "",
  "function_name": "two_sum_brute",
  "starter_code": "def two_sum_brute(nums, target):\n    pass\n",
  "parameters": [
    {
      "name": "nums",
      "type": "List[int]"
    },
    {
      "name": "target",
      "type": "int"
    }
  ],
  "expected_return_type": "List[int]",
  "comparison": "",
  "visible_tests": [
    {
      "args": [
        [
          2,
          7,
          11,
          15
        ],
        9
      ],
      "expected": [
        0,
        1
      ]
    },
    {
      "args": [
        [
          1,
          1
        ],
        2
      ],
      "expected": [
        0,
        1
      ]
    }
  ],
  "hidden_tests": [
    {
      "args": [
        [
          1,
          2
        ],
        5
      ],
      "expected": []
    }
  ],
  "hint_plan": {
    "level_1": "Use nested loops: for each `i`, try `j` with `j > i`.",
    "level_2": "Return as soon as you find a pair that sums to `target`.",
    "level_3": "You only need plain loops here\u2014focus on correct bounds and early exit.",
    "level_4": "Return `[]` when no pair works."
  },
  "canonical_solution_summary": "brute",
  "disallowed_full_solution_exposure": true,
  "skill_tags": [
    "lists",
    "loops"
  ],
  "tags": [
    "precode",
    "dicts-sets"
  ]
}
