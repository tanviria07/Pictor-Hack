{
  "id": "swim-in-rising-water",
  "title": "Swim in Rising Water",
  "difficulty": "hard",
  "category": "advanced-graphs",
  "description": "You are given an `n x n` integer matrix `grid` where each value `grid[i][j]` represents the elevation at that point `(i, j)`.\n\nIt starts raining, and water gradually rises over time. At time `t`, the water level is `t`, meaning any cell with elevation less than equal to `t` is submerged or reachable.\n\nYou can swim from a square to another 4-directionally adjacent square if and only if the elevation of both squares individually are at most `t`. You can swim infinite distances in zero time. Of course, you must stay within the boundaries of the grid during your swim.\n\nReturn the minimum time until you can reach the bottom right square `(n - 1, n - 1)` if you start at the top left square `(0, 0)`.\n\n \n\nExample 1:\n\nInput: grid = [[0,2],[1,3]]\nOutput: 3\nExplanation:\nAt time 0, you are in grid location (0, 0).\nYou cannot go anywhere else because 4-directionally adjacent neighbors have a higher elevation than t = 0.\nYou cannot reach point (1, 1) until time 3.\nWhen the depth of water is 3, we can swim anywhere inside the grid.\n\nExample 2:\n\nInput: grid = [[0,1,2,3,4],[24,23,22,21,5],[12,13,14,15,16],[11,17,18,19,20],[10,9,8,7,6]]\nOutput: 16\nExplanation: The final route is shown.\nWe need to wait until time 16 so that (0, 0) and (4, 4) are connected.",
  "examples": [
    {
      "input": "grid = [[0,2],[1,3]]",
      "output": "3",
      "explanation": "At time 0, you are in grid location (0, 0).\nYou cannot go anywhere else because 4-directionally adjacent neighbors have a higher elevation than t = 0.\nYou cannot reach point (1, 1) until time 3.\nWhen the depth of water is 3, we can swim anywhere inside the grid."
    },
    {
      "input": "grid = [[0,1,2,3,4],[24,23,22,21,5],[12,13,14,15,16],[11,17,18,19,20],[10,9,8,7,6]]",
      "output": "16",
      "explanation": "The final route is shown.\nWe need to wait until time 16 so that (0, 0) and (4, 4) are connected."
    }
  ],
  "constraints": [
    "`n == grid.length`",
    "`n == grid[i].length`",
    "`1 <= n <= 50`",
    "`0 <= grid[i][j] < n^2`",
    "Each value `grid[i][j]` is unique."
  ],
  "execution_mode": "function",
  "class_name": "",
  "function_name": "swim_in_water",
  "starter_code": "def swim_in_water(grid):\n    pass\n",
  "parameters": [
    {
      "name": "grid",
      "type": "List[List[int]]"
    }
  ],
  "expected_return_type": "int",
  "comparison": "",
  "visible_tests": [
    {
      "args": [
        [
          [
            0,
            2
          ],
          [
            1,
            3
          ]
        ]
      ],
      "expected": 3
    },
    {
      "args": [
        [
          [
            0,
            1,
            2,
            3,
            4
          ],
          [
            24,
            23,
            22,
            21,
            5
          ],
          [
            12,
            13,
            14,
            15,
            16
          ],
          [
            11,
            17,
            18,
            19,
            20
          ],
          [
            10,
            9,
            8,
            7,
            6
          ]
        ]
      ],
      "expected": 16
    }
  ],
  "hidden_tests": [],
  "hint_plan": {
    "level_1": "Use either Dijkstra's, or binary search for the best time T for which you can reach the end if you only step on squares at most T.",
    "level_2": "Use either Dijkstra's, or binary search for the best time T for which you can reach the end if you only step on squares at most T.",
    "level_3": "Use either Dijkstra's, or binary search for the best time T for which you can reach the end if you only step on squares at most T.",
    "level_4": "Use either Dijkstra's, or binary search for the best time T for which you can reach the end if you only step on squares at most T."
  },
  "canonical_solution_summary": "Use either Dijkstra's, or binary search for the best time T for which you can reach the end if you only step on squares at most T.",
  "disallowed_full_solution_exposure": true,
  "solution_sentences": [
    "def swim_in_water(grid):",
    "    n = len(grid)",
    "    left, right = 0, n * n - 1",
    "    while left < right:",
    "        mid = (left + right) // 2",
    "        if can_reach(grid, mid):",
    "            right = mid",
    "        else:",
    "            left = mid + 1",
    "    return left",
    "def can_reach(grid, t):",
    "    n = len(grid)",
    "    visited = [[False] * n for _ in range(n)]",
    "    stack = [(0, 0)]",
    "    if grid[0][0] > t:",
    "        return False",
    "    visited[0][0] = True",
    "    while stack:",
    "        i, j = stack.pop()",
    "        if i == n - 1 and j == n - 1:",
    "            return True",
    "        for di, dj in [(1, 0), (-1, 0), (0, 1), (0, -1)]:",
    "            ni, nj = i + di, j + dj",
    "            if 0 <= ni < n and 0 <= nj < n and not visited[ni][nj] and grid[ni][nj] <= t:",
    "                visited[ni][nj] = True",
    "                stack.append((ni, nj))",
    "    return False"
  ],
  "hints_per_sentence": [
    "Start by defining the function with the given parameter.",
    "Get the size of the grid to use later for bounds.",
    "Initialize the binary search range: the minimum possible time is 0, and the maximum is the highest elevation in the grid (which is at most n² - 1).",
    "Set up a loop to narrow down the minimum time using binary search.",
    "Compute the midpoint of the current search range.",
    "Check if it's possible to reach the bottom-right corner when the water level is at this midpoint.",
    "If reachable, adjust the search to look for smaller times.",
    "If not reachable, adjust to look for larger times.",
    "After the loop, the left pointer holds the minimum time.",
    "Return that value.",
    "Define a helper function to test reachability for a given time t.",
    "Again, get the grid size for bounds.",
    "Create a visited matrix to track which cells have been explored.",
    "Initialize a stack for DFS starting from the top-left corner.",
    "First, check if the starting cell is accessible at time t; if not, return False immediately.",
    "Mark the starting cell as visited.",
    "Loop while there are cells to explore in the stack.",
    "Pop the current cell coordinates from the stack.",
    "If this cell is the bottom-right corner, return True since we've reached it.",
    "Iterate over the four possible directions (down, up, right, left).",
    "Calculate the coordinates of the neighbor cell.",
    "If the neighbor is within bounds, unvisited, and has elevation at most t, mark it visited and add it to the stack.",
    "If the loop ends without reaching the target, return False.",
    "",
    "",
    "",
    ""
  ],
  "final_explanation": "The solution uses binary search to find the minimum time T such that you can travel from (0,0) to (n-1,n-1) only stepping on cells with elevation ≤ T. For each candidate T, a DFS (or BFS) checks reachability by exploring accessible cells. This approach efficiently narrows down T by halving the search range each time, avoiding checking all possible times directly."
}
