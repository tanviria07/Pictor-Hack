{
  "id": "product-of-array-except-self",
  "title": "Product of Array Except Self",
  "difficulty": "medium",
  "category": "arrays-hashing",
  "description": "Given an integer array `nums`, return an array `answer` such that `answer[i]` is equal to the product of all the elements of `nums` except `nums[i]`.\n\nThe product of any prefix or suffix of `nums` is guaranteed to fit in a 32-bit integer.\n\nYou must write an algorithm that runs in `O(n)` time and without using the division operation.\n\n \n\nExample 1:\n\nInput: nums = [1,2,3,4]\nOutput: [24,12,8,6]\n\nExample 2:\n\nInput: nums = [-1,1,0,-3,3]\nOutput: [0,0,9,0,0]",
  "examples": [
    {
      "input": "nums = [1,2,3,4]",
      "output": "[24,12,8,6]"
    },
    {
      "input": "nums = [-1,1,0,-3,3]",
      "output": "[0,0,9,0,0]"
    }
  ],
  "constraints": [
    "`2 <= nums.length <= 10^5`",
    "`-30 <= nums[i] <= 30`",
    "The input is generated such that `answer[i]` is guaranteed to fit in a 32-bit integer."
  ],
  "execution_mode": "function",
  "class_name": "",
  "function_name": "product_except_self",
  "starter_code": "def product_except_self(nums):\n    pass\n",
  "parameters": [
    {
      "name": "nums",
      "type": "List[int]"
    }
  ],
  "expected_return_type": "List[int]",
  "comparison": "",
  "visible_tests": [
    {
      "args": [
        [
          1,
          2,
          3,
          4
        ]
      ],
      "expected": [
        24,
        12,
        8,
        6
      ]
    },
    {
      "args": [
        [
          -1,
          1,
          0,
          -3,
          3
        ]
      ],
      "expected": [
        0,
        0,
        9,
        0,
        0
      ]
    }
  ],
  "hidden_tests": [],
  "hint_plan": {
    "level_1": "Think how you can efficiently utilize prefix and suffix products to calculate the product of all elements except self for each index. Can you pre-compute the prefix and suffix products in linear time to avoid redundant calculations?",
    "level_2": "Can you minimize additional space usage by reusing memory or modifying the input array to store intermediate results?",
    "level_3": "Can you minimize additional space usage by reusing memory or modifying the input array to store intermediate results?",
    "level_4": "Can you minimize additional space usage by reusing memory or modifying the input array to store intermediate results?"
  },
  "company_tags": ["Microsoft", "Google"],
  "canonical_solution_summary": "Can you minimize additional space usage by reusing memory or modifying the input array to store intermediate results?",
  "disallowed_full_solution_exposure": true,
  "solution_sentences": [
    "def product_except_self(nums):",
    "    n = len(nums)",
    "    answer = [1] * n",
    "    left_product = 1",
    "    for i in range(n):",
    "        answer[i] = left_product",
    "        left_product *= nums[i]",
    "    right_product = 1",
    "    for i in range(n - 1, -1, -1):",
    "        answer[i] *= right_product",
    "        right_product *= nums[i]",
    "    return answer"
  ],
  "hints_per_sentence": [
    "Start by defining the function with the given parameter.",
    "Calculate the length of the input list to use in loops.",
    "Initialize the result list with placeholders, setting each element to 1.",
    "Set up a variable to accumulate the product of elements from the left.",
    "Loop through each index from start to end.",
    "Assign the current accumulated left product to the corresponding position in the result.",
    "Update the left product by multiplying with the current element from the input.",
    "Set up a variable to accumulate the product of elements from the right.",
    "Loop through each index from end to start.",
    "Multiply the current result by the accumulated right product.",
    "Update the right product by multiplying with the current element from the input.",
    "Return the final result list."
  ],
  "final_explanation": "The solution uses two passes over the array: first, compute the product of all elements to the left of each index and store it in the result; second, compute the product of all elements to the right and multiply it with the stored left product. This avoids division and runs in O(n) time with O(1) extra space (excluding the output list)."
}
