{
  "id": "precode-pb-01-return-seven",
  "slug": "precode-pb-01-return-seven",
  "title": "Return Seven",
  "difficulty": "easy",
  "category": "precode-python-basics",
  "description": "Define a function named `answer` with no parameters. It should **return** the integer `7`. In Python, `print` shows output to the user; `return` hands a value back to the caller—here we only need `return`.",
  "examples": [
    {
      "input": "answer()",
      "output": "7"
    }
  ],
  "constraints": [
    "The function name must be exactly `answer`."
  ],
  "execution_mode": "function",
  "class_name": "",
  "function_name": "answer",
  "starter_code": "def answer():\n    pass\n",
  "parameters": [],
  "expected_return_type": "int",
  "comparison": "",
  "visible_tests": [
    {
      "args": [],
      "expected": 7
    }
  ],
  "hidden_tests": [
    {
      "args": [],
      "expected": 7
    }
  ],
  "hint_plan": {
    "level_1": "The tests call `answer()` and check the returned value.",
    "level_2": "Use a `return` statement, not `print`.",
    "level_3": "Return the integer 7: `return 7`.",
    "level_4": "Run passes when visible and hidden checks agree with the reference behavior."
  },
  "canonical_solution_summary": "return 7",
  "disallowed_full_solution_exposure": true,
  "skill_tags": [
    "variables",
    "functions"
  ],
  "tags": [
    "precode",
    "basics"
  ],
  "solution_sentences": [
    "def answer():",
    "    return 7"
  ],
  "hints_per_sentence": [
    "Define a function named `answer` with no parameters using `def answer():`.",
    "Inside the function, use `return 7` (not `print`)."
  ],
  "final_explanation": "`answer` is a zero-argument function that returns the integer 7. We use `return` (not `print`) because the tests check the value handed back to the caller, not what is printed.",
  "curated": true
}
