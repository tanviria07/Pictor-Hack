{
  "id": "valid-parenthesis-string",
  "title": "Valid Parenthesis String",
  "difficulty": "medium",
  "category": "greedy",
  "description": "Given a string `s` containing only three types of characters: `'('`, `')'` and `'*'`, return `true` if `s` is valid.\n\nThe following rules define a valid string:\n\n\t- Any left parenthesis `'('` must have a corresponding right parenthesis `')'`.\n\n\t- Any right parenthesis `')'` must have a corresponding left parenthesis `'('`.\n\n\t- Left parenthesis `'('` must go before the corresponding right parenthesis `')'`.\n\n\t- `'*'` could be treated as a single right parenthesis `')'` or a single left parenthesis `'('` or an empty string `\"\"`.\n\n \n\nExample 1:\n\nInput: s = \"()\"\nOutput: true\n\nExample 2:\n\nInput: s = \"(*)\"\nOutput: true\n\nExample 3:\n\nInput: s = \"(*))\"\nOutput: true",
  "examples": [
    {
      "input": "s = \"()\"",
      "output": "true"
    },
    {
      "input": "s = \"(*)\"",
      "output": "true"
    },
    {
      "input": "s = \"(*))\"",
      "output": "true"
    }
  ],
  "constraints": [
    "`1 <= s.length <= 100`",
    "`s[i]` is `'('`, `')'` or `'*'`."
  ],
  "execution_mode": "function",
  "class_name": "",
  "function_name": "check_valid_string",
  "starter_code": "def check_valid_string(s):\n    pass\n",
  "parameters": [
    {
      "name": "s",
      "type": "str"
    }
  ],
  "expected_return_type": "bool",
  "comparison": "",
  "visible_tests": [
    {
      "args": [
        "()"
      ],
      "expected": true
    },
    {
      "args": [
        "(*)"
      ],
      "expected": true
    },
    {
      "args": [
        "(*))"
      ],
      "expected": true
    }
  ],
  "hidden_tests": [],
  "hint_plan": {
    "level_1": "Use backtracking to explore all possible combinations of treating '*' as either '(', ')', or an empty string. If any combination leads to a valid string, return true.",
    "level_2": "DP[i][j] represents whether the substring s[i:j] is valid.",
    "level_3": "Keep track of the count of open parentheses encountered so far. If you encounter a close parenthesis, it should balance with an open parenthesis. Utilize a stack to handle this effectively.",
    "level_4": "How about using 2 stacks instead of 1? Think about it."
  },
  "canonical_solution_summary": "How about using 2 stacks instead of 1? Think about it.",
  "disallowed_full_solution_exposure": true,
  "recommended_for_roles": [
    "swe_intern",
    ""
  ]
}
